import { EventEmitter as EventBus } from 'events';
export default new EventBus();
// undateLayers ({ type = '', value = {} }) => {}
// currentItem (currentItem => {containerRef, targetRef, labelRef, node, style})
